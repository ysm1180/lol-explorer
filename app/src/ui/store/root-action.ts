import * as lcuActions from '../features/lcu/actions';
import * as riotApiActions from '../features/riot-api/actions';
import * as staticDataActions from '../features/static-data/actions';

export default {
  lcu: lcuActions,
  riotApi: riotApiActions,
  staticData: staticDataActions,
};
